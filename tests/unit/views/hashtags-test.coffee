`import { moduleFor, test } from 'ember-qunit'`

moduleFor 'view:hashtags', 'Unit | View | hashtags'

# Replace this with your real tests.
test 'it exists', (assert) ->
  view = @subject()
  assert.ok view
